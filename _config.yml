# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: StrumentalMente
email: "info@strumentalmente.it"
author: FSC
logo: "/assets/images/logo/1024x1024.png"
description: >-
  Il sito ufficiale.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://strumentalmente.it" # the base hostname & protocol for your site, e.g. http://example.com
# twitter_username: jekyllrb
github_username: F-S-C
lang: "it"
image: "/assets/images/logo/1024x1024.png"
canonical_url: 'https://strumentalmente.it'
author_url: "https://F-S-C.github.io"

# Build settings
markdown: kramdown
excerpt_separator: <!-- more -->
# theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      author: "FSC"
      layout: "post"
      imageurl: "http://placehold.it/700x400"

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Rakefile
  - "**/desktop.ini"
  - "desktop.ini"
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

sass:
  style: compressed
